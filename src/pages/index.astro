---
import BaseLayout from "@layouts/BaseLayout.astro";
import Tile from "@layouts/Tile.astro";
import MoreTile from "@layouts/MoreTile.astro";
import { getCollection, getDataEntryById } from "astro:content";

const allPosts = await getCollection("posts");
const allSeries = await getCollection("series");

allPosts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
allSeries.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<BaseLayout>
  <section class="everydayfeed">
    <div class="section-content">
      <h2 class="section-head">Latest Posts</h2>
      <ul role="list" class="section-tiles">
        {
          // tile-hero
          allPosts.slice(0, 1).map((post) => {
            return (
              <Tile
                title={post.data.title}
                href={`/posts/${post.slug}`}
                date={post.data.pubDate}
                tags={post.data.tags}
                cover={post.data.cover.url}
                level="1"
              />
            );
          })
        }

        {
          // tile-2up
          allPosts.slice(1, 5).map((post) => {
            return (
              <Tile
                title={post.data.title}
                href={`/posts/${post.slug}`}
                date={post.data.pubDate}
                tags={post.data.tags}
                cover={post.data.cover.url}
                level="2"
              />
            );
          })
        }

        {
          // tile-3up
          allPosts.slice(5, 11).map((post) => {
            return (
              <Tile
                title={post.data.title}
                href={`/posts/${post.slug}`}
                date={post.data.pubDate}
                tags={post.data.tags}
                cover={post.data.cover.url}
                level="3"
              />
            );
          })
        }
      </ul>
    </div>
  </section>

  <section class="more-from-newsroom">
    <div class="section-content">
      <h2 class="section-head">More Posts</h2>

      <ul role="list" class="section-tiles">
        {
          // tile-2up
          allPosts.slice(0, 6).map((post) => {
            return (
              <MoreTile
                title={post.data.title}
                href={`/posts/${post.slug}`}
                date={post.data.pubDate}
                tags={post.data.tags}
                cover={
                  post.data.cover.square !== ""
                    ? post.data.cover.square
                    : post.data.cover.url
                }
              />
            );
          })
        }
      </ul>
      <div class="view-archive-wrapper">
        <a
          href="/series"
          class="cta-primary-light"
          data-analytics-region="router"
          data-analytics-title="view archive">All Series</a
        >
      </div>
    </div>
  </section>

  <script is:inline>
    document.addEventListener("DOMContentLoaded", function () {
      var script = document.createElement("script");
      script.src = "/static/js/animation.js";
      document.head.appendChild(script);

      script.onload = function () {
        console.log("lazyload.js loaded");
        // when layout is loaded, load the images
        initImage();
      };
    });
  </script>
</BaseLayout>
